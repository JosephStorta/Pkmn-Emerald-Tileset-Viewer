# VSCode JSON files depend on the workspace settings
/.vscode

# Connected to the build process
/bin/**

# CMake build files depend on the compiler setup
/build/**

# CMake Presets depend on the build system
/CMakePresets.json
